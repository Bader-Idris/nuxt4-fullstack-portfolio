console.log('should be able to serve it using build:client ==============================> csr')
export default defineNuxtConfig({
  // TODO: how to run this??
  // TODO: we should use nuxt generate, but this should be the config file for it!
  ssr: false,
});