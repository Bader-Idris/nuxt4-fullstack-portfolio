diff --git a/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java b/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
index 4e8b554273a248e8e3e26c032f507e126907c054..b914fc3a4beb35dc34340ce8583be7187949c4e8 100644
--- a/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
+++ b/android/src/main/java/ee/forgr/capacitor/social/login/GoogleProvider.java
@@ -358,9 +358,7 @@ public class GoogleProvider implements SocialProvider {
             if (!nonce.isEmpty()) {
                 googleIdOptionBuilder.setNonce(nonce);
             }
-            if (filterByAuthorizedAccounts) {
-                googleIdOptionBuilder.setFilterByAuthorizedAccounts(true);
-            }
+            googleIdOptionBuilder.setFilterByAuthorizedAccounts(filterByAuthorizedAccounts);
 
             if (autoSelectEnabled) {
                 googleIdOptionBuilder.setAutoSelectEnabled(true);
@@ -654,6 +652,19 @@ public class GoogleProvider implements SocialProvider {
             }
         }
         if (e instanceof GetCredentialCancellationException) {
+            String message = e.getMessage();
+            if (message != null && message.contains("16") && isBottomUi) {
+                Log.e(LOG_TAG, "Account reauth failed with bottomUi. Switching to standard UI.");
+                try {
+                    options.put("style", "standard");
+                    call.getData().put("options", options);
+                } catch (JSONException ex) {
+                    call.reject("Google Sign-In failed: " + ex.getMessage());
+                    return;
+                }
+                login(call, config);
+                return;
+            }
             call.reject("Google Sign-In cancelled by user", USER_CANCELLED_CODE, e);
             return;
         }
